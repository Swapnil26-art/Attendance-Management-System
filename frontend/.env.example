# Leave empty to use relative "/api" (works in dev via Vite proxy and on Vercel same-origin).
# If the backend is hosted separately, set its full URL, e.g.:
# VITE_API_URL=https://your-backend.vercel.app/api
VITE_API_URL=